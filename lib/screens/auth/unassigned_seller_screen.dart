import 'package:flutter/material.dart';

class UnassignedSellerScreen extends StatelessWidget {
  const UnassignedSellerScreen();

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}